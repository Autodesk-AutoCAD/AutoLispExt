(defun writeascii () 
  (setq f (open "/user/td/ascii.txt" "w"))
  ;format should not remove the empty line after and before

  (setq test 26)
)

(defun writea () 

  (setq f (open "/user/td/ascii.txt" "w"))

  (setq test 26)
)


